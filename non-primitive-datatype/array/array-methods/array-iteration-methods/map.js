// create a array upline a function to each element transform the array without changing the orginal array and its retturn new array transformd values

const double = [1,2,3].map(num => num * 2);
console.log(double);
